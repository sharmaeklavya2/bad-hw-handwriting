# Bad Homework Handwriting

Website documenting examples of bad handwriting I have come across while grading homeworks.

## How to build and deploy

1.  Install dependencies: `pip install -r requirements.txt`.
2.  Create `index.html`: `python3 render.py`

Then serve the repository's root directory using an HTTP server.
